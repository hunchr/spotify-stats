<table>
  <thead>
    <tr>
      <th><%= t "songs.rank" %></th>
      <% SongsController::SORT.each do |sort, dir| %>
        <th><%= link_to t("songs.#{sort}"), songs_path_sort(sort, dir) %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @songs.each_with_index do |song, index| %>
      <tr>
        <td><%= index.next + page_offset %></td>
        <td><%= song.name %></td>
        <td class="max-w-12"><%= song.artist_name %></td>
        <td class="text-right"><%= song.plays_count %></td>
        <td><%= song.first_played_at[..9] %></td>
        <td><%= song.last_played_at[..9] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
